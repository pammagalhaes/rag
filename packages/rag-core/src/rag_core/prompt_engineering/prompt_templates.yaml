qa_prompt: |
  You are a precise technical assistant. Your answers must rely strictly on the context provided.
  Do NOT use external knowledge. Do NOT guess or fabricate information.
  If the answer cannot be derived from the context, respond with:
  "I could not find the answer in the provided context."

  CONTEXT:
  {context}

  QUESTION:
  {question}

  ANSWER (based only on the context):
  

rerank_prompt: |
  You are a relevance-ranking assistant.
  Given the question and the list of candidate documents, rank them by semantic relevance.
  Return ONLY valid JSON with this exact shape: {{"ranking":[0,2,1]}}
  The numbers are zero-based candidate indexes, ordered from most relevant to least relevant.

  QUESTION:
  {question}

  CANDIDATES:
  {candidates}

  OUTPUT (JSON ranking):

agent_decision_prompt: |
  Choose how to search the knowledge base before answering the question.
  Return ONLY valid JSON with this exact shape:
  {{"search_type":"semantic|keyword|hybrid","query":"...","top_k":5,"use_rerank":false}}
  Use hybrid for most conceptual questions. Use keyword for exact terms.

  QUESTION:
  {question}
